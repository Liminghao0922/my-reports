# React + Vite Environment Variables
# Copy this file to .env for local development
# All variables MUST be prefixed with VITE_ to be accessible in the browser

# ============================================================================
# Power BI Configuration (Required)
# ============================================================================

# Azure AD App Registration (SPA type)
# Get these from: Azure Portal → App registrations → Your app → Overview
VITE_POWERBI_CLIENT_ID=your-client-id-here
VITE_POWERBI_TENANT_ID=your-tenant-id-here

# Power BI Workspace Configuration
# Get this from: Power BI Service → Workspace → Settings
VITE_POWERBI_WORKSPACE_ID=your-workspace-id-here

# ============================================================================
# Looker Studio Configuration (Optional)
# ============================================================================

# Looker Studio Embed URL
# Get this from: Looker Studio → Share → Embed
VITE_LOOKER_EMBED_URL=https://lookerstudio.google.com/embed/reporting/your-report-id

# Optional: Looker Dashboard ID
VITE_LOOKER_DASHBOARD_ID=your-dashboard-id-here

# Looker Authentication Method (default: public)
# - public: No authentication required
# - required: User must sign in, but no user context passed
# - sso: Pass user email/name to Looker via URL parameters
# - filter: Use Row-Level Security with user email filter (params.user_email)
VITE_LOOKER_AUTH_METHOD=public

# ============================================================================
# ⚠️ Security Notes
# ============================================================================
# - NEVER commit .env file with real credentials!
# - Variables prefixed with VITE_ are embedded in client-side JavaScript
# - Do NOT put sensitive secrets here - they will be visible in browser
# - No client secret needed - MSAL uses browser-based public client flow
# - Ensure .env is in your .gitignore (it is by default)

# ============================================================================
# Production Deployment
# ============================================================================
# In Azure Static Web Apps, configure these as environment variables:
# 1. Go to Azure Portal → Static Web Apps → Configuration → Environment variables
# 2. Add the same variables WITHOUT the VITE_ prefix
# 3. GitHub Actions workflow will add VITE_ prefix during build

# ============================================================================
# Example Values (for testing only - replace with your actual values)
# ============================================================================
# VITE_POWERBI_CLIENT_ID=12345678-1234-1234-1234-123456789012
# VITE_POWERBI_TENANT_ID=87654321-4321-4321-4321-210987654321
# VITE_POWERBI_WORKSPACE_ID=abcdef12-3456-7890-abcd-ef1234567890

